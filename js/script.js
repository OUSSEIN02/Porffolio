/* ============ typing ============== */

const typed = new Typed('.typing', {
    strings:['', 'Développeur Front-end', 'Développeur Backend', ],
    typeSpeed: 100,
    BackSpeed: 60,
    loop:true
})